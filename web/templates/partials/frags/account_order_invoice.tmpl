{{ define "frag_account_order_invoice" }}
{{ $view := . }}
<div class="space-y-6 p-5">
  <section class="space-y-3 rounded-2xl border border-gray-200 bg-white p-5 shadow-sm">
    <header class="flex flex-wrap items-center justify-between gap-3">
      <div>
        <h3 class="text-sm font-semibold text-gray-900">{{ if eq $view.Lang "ja" }}ドキュメント{{ else }}Documents{{ end }}</h3>
        <p class="text-xs text-gray-500">{{ if eq $view.Lang "ja" }}請求書・ラベル・添付ファイル{{ else }}Invoices, labels, and attachments{{ end }}</p>
      </div>
      <span class="inline-flex items-center gap-1 rounded-full bg-gray-100 px-3 py-1 text-xs text-gray-600">
        {{ len $view.Documents }} {{ if eq $view.Lang "ja" }}件{{ else }}files{{ end }}
      </span>
    </header>
    <div class="grid gap-3 md:grid-cols-2">
      {{ range $doc := $view.Documents }}
        <article class="space-y-2 rounded-2xl border border-gray-100 bg-gray-50 px-4 py-3 text-sm text-gray-700">
          <div class="flex items-center justify-between gap-2">
            <div>
              <p class="text-xs font-semibold uppercase tracking-wide text-gray-500">{{ $doc.Type }}</p>
              <p class="text-sm font-semibold text-gray-900">{{ $doc.Label }}</p>
            </div>
            <span class="inline-flex items-center rounded-full bg-white px-3 py-1 text-[11px] font-semibold uppercase tracking-wide text-gray-700 shadow">{{ $doc.Status }}</span>
          </div>
          <p class="text-xs text-gray-500">{{ if eq $view.Lang "ja" }}更新{{ else }}Updated{{ end }} {{ fmtDate $doc.UpdatedAt $view.Lang }} · {{ $doc.Size }}</p>
          <a href="{{ $doc.URL }}" class="inline-flex items-center gap-1 text-xs font-semibold text-indigo-600 hover:text-indigo-500">
            {{ template "icon" (dict "Name" "arrow-down-tray" "Class" "h-4 w-4") }}
            {{ if eq $view.Lang "ja" }}ダウンロード{{ else }}Download{{ end }}
          </a>
        </article>
      {{ end }}
    </div>
  </section>

  <div class="grid gap-4 lg:grid-cols-[minmax(0,1fr)_minmax(0,1fr)]">
    <section class="space-y-3 rounded-2xl border border-gray-200 bg-white p-5 shadow-sm">
      <header class="flex items-center justify-between gap-3">
        <h3 class="text-sm font-semibold text-gray-900">{{ if eq $view.Lang "ja" }}送付先{{ else }}Recipients{{ end }}</h3>
        <span class="text-xs text-gray-500">{{ len $view.Recipients }}</span>
      </header>
      <ul class="space-y-3 text-sm text-gray-700">
        {{ range $recipient := $view.Recipients }}
          <li class="rounded-2xl border border-gray-100 bg-gray-50 px-4 py-3">
            <p class="text-xs font-semibold uppercase tracking-wide text-gray-500">{{ $recipient.Role }}</p>
            <p class="text-sm font-semibold text-gray-900">{{ $recipient.Name }}</p>
            <p class="text-xs text-gray-500">{{ $recipient.Email }}</p>
            {{ if $recipient.Phone }}<p class="text-xs text-gray-500">{{ $recipient.Phone }}</p>{{ end }}
            <p class="mt-1 text-[11px] text-indigo-600">{{ $recipient.Status }}</p>
          </li>
        {{ end }}
      </ul>
    </section>

    <section class="space-y-3 rounded-2xl border border-gray-200 bg-white p-5 shadow-sm">
      <header class="flex items-center justify-between gap-3">
        <h3 class="text-sm font-semibold text-gray-900">{{ if eq $view.Lang "ja" }}監査履歴{{ else }}Audit trail{{ end }}</h3>
        <span class="text-xs text-gray-500">{{ len $view.History }}</span>
      </header>
      <ol class="space-y-3 text-sm">
        {{ range $event := $view.History }}
          <li class="rounded-2xl border border-gray-100 bg-gray-50 px-4 py-3">
            <div class="flex items-center gap-2 text-xs font-semibold uppercase tracking-wide text-gray-500">
              {{ template "icon" (dict "Name" $event.Icon "Class" "h-4 w-4 text-gray-400") }}
              <span>{{ $event.Title }}</span>
            </div>
            <p class="mt-2 text-sm text-gray-700">{{ $event.Body }}</p>
            <p class="mt-2 text-xs text-gray-500">{{ fmtDate $event.Timestamp $view.Lang }} · {{ $event.Actor }}</p>
          </li>
        {{ end }}
      </ol>
    </section>
  </div>

  <p class="rounded-2xl border border-dashed border-indigo-200 bg-indigo-50/60 px-4 py-3 text-xs text-indigo-900">{{ $view.Message }}</p>
</div>
{{ end }}
