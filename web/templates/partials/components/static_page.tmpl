{{ define "static_page" }}
<section id="content-page" class="py-12">
  <div class="mx-auto w-full max-w-6xl space-y-10 px-4">
    {{ template "content_header" dict "Lang" $.Lang "Header" $.Content.Header }}

    {{ if $.Content.Locales }}
      {{ template "locale_switcher" dict "Lang" $.Lang "Locales" $.Content.Locales }}
    {{ end }}

    {{ if $.Content.Banner }}
      {{ template "alert_banner" dict "Lang" $.Lang "Banner" $.Content.Banner }}
    {{ end }}

    <div class="grid gap-12 lg:grid-cols-[minmax(0,3fr),minmax(0,1fr)]">
      <article class="space-y-8">
        <div class="content-prose">
          {{ $.Content.Body }}
        </div>
        {{ if $.Content.Version }}
          {{ template "version_footer" dict "Lang" $.Lang "Version" $.Content.Version }}
        {{ end }}
        {{ if $.Content.History }}
          {{ template "version_table" dict "Lang" $.Lang "History" $.Content.History }}
        {{ end }}
        {{ if $.Content.Feedback }}
          {{ template "was_helpful" dict "Lang" $.Lang "Feedback" $.Content.Feedback }}
        {{ end }}
        {{ if $.Content.Footer }}
          {{ template "legal_footer" dict "Lang" $.Lang "Footer" $.Content.Footer }}
        {{ end }}
      </article>
      {{ if $.Content.TOC }}
        <aside class="hidden lg:block">
          {{ template "content_toc" dict "Lang" $.Lang "TOC" $.Content.TOC }}
        </aside>
      {{ end }}
    </div>

    {{ if $.Content.TOC }}
      <div class="lg:hidden">
        {{ template "content_toc" dict "Lang" $.Lang "TOC" $.Content.TOC }}
      </div>
    {{ end }}
  </div>
</section>
{{ end }}
